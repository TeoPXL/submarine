version: "3.9"
services:
  ros2:
    build: ros2/.
    container_name: ros2_submarine
    network_mode: bridge
    ports:
      - "10000:10000"   # Unity ROS TCP Connector
    volumes:
      - ./ros2_ws:/ros2_ws
    tty: true

